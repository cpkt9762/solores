//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-06
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

use super::*;
///Closes a `VaultTransaction` and the corresponding `Proposal`.
///`transaction` can be closed if either:
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
pub const VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN: usize = 5;
///Accounts struct for the vaultTransactionAccountsClose instruction
#[derive(Copy, Clone, Debug)]
pub struct VaultTransactionAccountsCloseAccounts<'me, 'info> {
    pub multisig: &'me AccountInfo<'info>,
    pub proposal: &'me AccountInfo<'info>,
    ///VaultTransaction corresponding to the `proposal`.
    pub transaction: &'me AccountInfo<'info>,
    ///The rent collector.
    pub rent_collector: &'me AccountInfo<'info>,
    pub system_program: &'me AccountInfo<'info>,
}
///Public keys struct for the vaultTransactionAccountsClose instruction
#[derive(Copy, Clone, Debug, PartialEq)]
pub struct VaultTransactionAccountsCloseKeys {
    pub multisig: Pubkey,
    pub proposal: Pubkey,
    ///VaultTransaction corresponding to the `proposal`.
    pub transaction: Pubkey,
    ///The rent collector.
    pub rent_collector: Pubkey,
    pub system_program: Pubkey,
}
impl From<VaultTransactionAccountsCloseAccounts<'_, '_>> for VaultTransactionAccountsCloseKeys {
    fn from(accounts: VaultTransactionAccountsCloseAccounts) -> Self {
        Self {
            multisig: *accounts.multisig.key,
            proposal: *accounts.proposal.key,
            transaction: *accounts.transaction.key,
            rent_collector: *accounts.rent_collector.key,
            system_program: *accounts.system_program.key,
        }
    }
}
impl From<VaultTransactionAccountsCloseKeys>
    for [AccountMeta; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(keys: VaultTransactionAccountsCloseKeys) -> Self {
        [
            AccountMeta {
                pubkey: keys.multisig,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.proposal,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.transaction,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.rent_collector,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.system_program,
                is_signer: false,
                is_writable: false,
            },
        ]
    }
}
impl From<[Pubkey; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]>
    for VaultTransactionAccountsCloseKeys
{
    fn from(pubkeys: [Pubkey; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            multisig: pubkeys[0],
            proposal: pubkeys[1],
            transaction: pubkeys[2],
            rent_collector: pubkeys[3],
            system_program: pubkeys[4],
        }
    }
}
impl From<&[String]> for VaultTransactionAccountsCloseKeys {
    fn from(pubkeys: &[String]) -> Self {
        Self {
            multisig: pubkeys[0].parse().unwrap(),
            proposal: pubkeys[1].parse().unwrap(),
            transaction: pubkeys[2].parse().unwrap(),
            rent_collector: pubkeys[3].parse().unwrap(),
            system_program: pubkeys[4].parse().unwrap(),
        }
    }
}
impl<'info> From<VaultTransactionAccountsCloseAccounts<'_, 'info>>
    for [AccountInfo<'info>; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(accounts: VaultTransactionAccountsCloseAccounts<'_, 'info>) -> Self {
        [
            accounts.multisig.clone(),
            accounts.proposal.clone(),
            accounts.transaction.clone(),
            accounts.rent_collector.clone(),
            accounts.system_program.clone(),
        ]
    }
}
impl<'me, 'info> From<&'me [AccountInfo<'info>; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]>
    for VaultTransactionAccountsCloseAccounts<'me, 'info>
{
    fn from(
        arr: &'me [AccountInfo<'info>; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN],
    ) -> Self {
        Self {
            multisig: &arr[0],
            proposal: &arr[1],
            transaction: &arr[2],
            rent_collector: &arr[3],
            system_program: &arr[4],
        }
    }
}
pub const VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM: u8 = 28u8;
#[derive(Clone, Debug, PartialEq)]
pub struct VaultTransactionAccountsCloseIxData;
impl VaultTransactionAccountsCloseIxData {
    pub fn deserialize(buf: &[u8]) -> std::io::Result<Self> {
        if buf.is_empty() {
            return Err(std::io::Error::new(
                std::io::ErrorKind::InvalidData,
                "Instruction data is empty",
            ));
        }
        let maybe_discm = buf[0];
        if maybe_discm != VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM {
            return Err(std::io::Error::new(
                std::io::ErrorKind::Other,
                format!(
                    "discm does not match. Expected: {}. Received: {}",
                    VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM, maybe_discm
                ),
            ));
        }
        Ok(Self)
    }
    pub fn serialize<W: std::io::Write>(&self, mut writer: W) -> std::io::Result<()> {
        writer.write_all(&[VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM])
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        let mut data = Vec::new();
        self.serialize(&mut data)?;
        Ok(data)
    }
}
pub fn vault_transaction_accounts_close_ix_with_program_id(
    program_id: Pubkey,
    keys: VaultTransactionAccountsCloseKeys,
) -> std::io::Result<Instruction> {
    let metas: [AccountMeta; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN] = keys.into();
    Ok(Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: VaultTransactionAccountsCloseIxData.try_to_vec()?,
    })
}
pub fn vault_transaction_accounts_close_ix(
    keys: VaultTransactionAccountsCloseKeys,
) -> std::io::Result<Instruction> {
    vault_transaction_accounts_close_ix_with_program_id(crate::ID, keys)
}
pub fn vault_transaction_accounts_close_invoke_with_program_id(
    program_id: Pubkey,
    accounts: VaultTransactionAccountsCloseAccounts<'_, '_>,
) -> ProgramResult {
    let keys: VaultTransactionAccountsCloseKeys = accounts.into();
    let ix = vault_transaction_accounts_close_ix_with_program_id(program_id, keys)?;
    invoke_instruction(&ix, accounts)
}
pub fn vault_transaction_accounts_close_invoke(
    accounts: VaultTransactionAccountsCloseAccounts<'_, '_>,
) -> ProgramResult {
    vault_transaction_accounts_close_invoke_with_program_id(crate::ID, accounts)
}
pub fn vault_transaction_accounts_close_invoke_signed_with_program_id(
    program_id: Pubkey,
    accounts: VaultTransactionAccountsCloseAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    let keys: VaultTransactionAccountsCloseKeys = accounts.into();
    let ix = vault_transaction_accounts_close_ix_with_program_id(program_id, keys)?;
    invoke_instruction_signed(&ix, accounts, seeds)
}
pub fn vault_transaction_accounts_close_invoke_signed(
    accounts: VaultTransactionAccountsCloseAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    vault_transaction_accounts_close_invoke_signed_with_program_id(crate::ID, accounts, seeds)
}
pub fn vault_transaction_accounts_close_verify_account_keys(
    accounts: VaultTransactionAccountsCloseAccounts<'_, '_>,
    keys: VaultTransactionAccountsCloseKeys,
) -> Result<(), (Pubkey, Pubkey)> {
    for (actual, expected) in [
        (*accounts.multisig.key, keys.multisig),
        (*accounts.proposal.key, keys.proposal),
        (*accounts.transaction.key, keys.transaction),
        (*accounts.rent_collector.key, keys.rent_collector),
        (*accounts.system_program.key, keys.system_program),
    ] {
        if actual != expected {
            return Err((actual, expected));
        }
    }
    Ok(())
}
