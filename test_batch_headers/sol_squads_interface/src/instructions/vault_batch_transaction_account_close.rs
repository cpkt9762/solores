//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-06
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

use super::*;
///Closes a `VaultBatchTransaction` belonging to the `batch` and `proposal`.
///`transaction` can be closed if either:
///- it's marked as executed within the `batch`;
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
pub const VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN: usize = 6;
///Accounts struct for the vaultBatchTransactionAccountClose instruction
#[derive(Copy, Clone, Debug)]
pub struct VaultBatchTransactionAccountCloseAccounts<'me, 'info> {
    pub multisig: &'me AccountInfo<'info>,
    pub proposal: &'me AccountInfo<'info>,
    ///`Batch` corresponding to the `proposal`.
    pub batch: &'me AccountInfo<'info>,
    ///`VaultBatchTransaction` account to close.
    ///The transaction must be the current last one in the batch.
    pub transaction: &'me AccountInfo<'info>,
    ///The rent collector.
    pub rent_collector: &'me AccountInfo<'info>,
    pub system_program: &'me AccountInfo<'info>,
}
///Public keys struct for the vaultBatchTransactionAccountClose instruction
#[derive(Copy, Clone, Debug, PartialEq)]
pub struct VaultBatchTransactionAccountCloseKeys {
    pub multisig: Pubkey,
    pub proposal: Pubkey,
    ///`Batch` corresponding to the `proposal`.
    pub batch: Pubkey,
    ///`VaultBatchTransaction` account to close.
    ///The transaction must be the current last one in the batch.
    pub transaction: Pubkey,
    ///The rent collector.
    pub rent_collector: Pubkey,
    pub system_program: Pubkey,
}
impl From<VaultBatchTransactionAccountCloseAccounts<'_, '_>>
    for VaultBatchTransactionAccountCloseKeys
{
    fn from(accounts: VaultBatchTransactionAccountCloseAccounts) -> Self {
        Self {
            multisig: *accounts.multisig.key,
            proposal: *accounts.proposal.key,
            batch: *accounts.batch.key,
            transaction: *accounts.transaction.key,
            rent_collector: *accounts.rent_collector.key,
            system_program: *accounts.system_program.key,
        }
    }
}
impl From<VaultBatchTransactionAccountCloseKeys>
    for [AccountMeta; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(keys: VaultBatchTransactionAccountCloseKeys) -> Self {
        [
            AccountMeta {
                pubkey: keys.multisig,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.proposal,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.batch,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.transaction,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.rent_collector,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.system_program,
                is_signer: false,
                is_writable: false,
            },
        ]
    }
}
impl From<[Pubkey; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]>
    for VaultBatchTransactionAccountCloseKeys
{
    fn from(pubkeys: [Pubkey; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            multisig: pubkeys[0],
            proposal: pubkeys[1],
            batch: pubkeys[2],
            transaction: pubkeys[3],
            rent_collector: pubkeys[4],
            system_program: pubkeys[5],
        }
    }
}
impl From<&[String]> for VaultBatchTransactionAccountCloseKeys {
    fn from(pubkeys: &[String]) -> Self {
        Self {
            multisig: pubkeys[0].parse().unwrap(),
            proposal: pubkeys[1].parse().unwrap(),
            batch: pubkeys[2].parse().unwrap(),
            transaction: pubkeys[3].parse().unwrap(),
            rent_collector: pubkeys[4].parse().unwrap(),
            system_program: pubkeys[5].parse().unwrap(),
        }
    }
}
impl<'info> From<VaultBatchTransactionAccountCloseAccounts<'_, 'info>>
    for [AccountInfo<'info>; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(accounts: VaultBatchTransactionAccountCloseAccounts<'_, 'info>) -> Self {
        [
            accounts.multisig.clone(),
            accounts.proposal.clone(),
            accounts.batch.clone(),
            accounts.transaction.clone(),
            accounts.rent_collector.clone(),
            accounts.system_program.clone(),
        ]
    }
}
impl<'me, 'info>
    From<&'me [AccountInfo<'info>; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]>
    for VaultBatchTransactionAccountCloseAccounts<'me, 'info>
{
    fn from(
        arr: &'me [AccountInfo<'info>; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN],
    ) -> Self {
        Self {
            multisig: &arr[0],
            proposal: &arr[1],
            batch: &arr[2],
            transaction: &arr[3],
            rent_collector: &arr[4],
            system_program: &arr[5],
        }
    }
}
pub const VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM: u8 = 29u8;
#[derive(Clone, Debug, PartialEq)]
pub struct VaultBatchTransactionAccountCloseIxData;
impl VaultBatchTransactionAccountCloseIxData {
    pub fn deserialize(buf: &[u8]) -> std::io::Result<Self> {
        if buf.is_empty() {
            return Err(std::io::Error::new(
                std::io::ErrorKind::InvalidData,
                "Instruction data is empty",
            ));
        }
        let maybe_discm = buf[0];
        if maybe_discm != VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM {
            return Err(std::io::Error::new(
                std::io::ErrorKind::Other,
                format!(
                    "discm does not match. Expected: {}. Received: {}",
                    VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM, maybe_discm
                ),
            ));
        }
        Ok(Self)
    }
    pub fn serialize<W: std::io::Write>(&self, mut writer: W) -> std::io::Result<()> {
        writer.write_all(&[VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM])
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        let mut data = Vec::new();
        self.serialize(&mut data)?;
        Ok(data)
    }
}
pub fn vault_batch_transaction_account_close_ix_with_program_id(
    program_id: Pubkey,
    keys: VaultBatchTransactionAccountCloseKeys,
) -> std::io::Result<Instruction> {
    let metas: [AccountMeta; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN] = keys.into();
    Ok(Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: VaultBatchTransactionAccountCloseIxData.try_to_vec()?,
    })
}
pub fn vault_batch_transaction_account_close_ix(
    keys: VaultBatchTransactionAccountCloseKeys,
) -> std::io::Result<Instruction> {
    vault_batch_transaction_account_close_ix_with_program_id(crate::ID, keys)
}
pub fn vault_batch_transaction_account_close_invoke_with_program_id(
    program_id: Pubkey,
    accounts: VaultBatchTransactionAccountCloseAccounts<'_, '_>,
) -> ProgramResult {
    let keys: VaultBatchTransactionAccountCloseKeys = accounts.into();
    let ix = vault_batch_transaction_account_close_ix_with_program_id(program_id, keys)?;
    invoke_instruction(&ix, accounts)
}
pub fn vault_batch_transaction_account_close_invoke(
    accounts: VaultBatchTransactionAccountCloseAccounts<'_, '_>,
) -> ProgramResult {
    vault_batch_transaction_account_close_invoke_with_program_id(crate::ID, accounts)
}
pub fn vault_batch_transaction_account_close_invoke_signed_with_program_id(
    program_id: Pubkey,
    accounts: VaultBatchTransactionAccountCloseAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    let keys: VaultBatchTransactionAccountCloseKeys = accounts.into();
    let ix = vault_batch_transaction_account_close_ix_with_program_id(program_id, keys)?;
    invoke_instruction_signed(&ix, accounts, seeds)
}
pub fn vault_batch_transaction_account_close_invoke_signed(
    accounts: VaultBatchTransactionAccountCloseAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    vault_batch_transaction_account_close_invoke_signed_with_program_id(crate::ID, accounts, seeds)
}
pub fn vault_batch_transaction_account_close_verify_account_keys(
    accounts: VaultBatchTransactionAccountCloseAccounts<'_, '_>,
    keys: VaultBatchTransactionAccountCloseKeys,
) -> Result<(), (Pubkey, Pubkey)> {
    for (actual, expected) in [
        (*accounts.multisig.key, keys.multisig),
        (*accounts.proposal.key, keys.proposal),
        (*accounts.batch.key, keys.batch),
        (*accounts.transaction.key, keys.transaction),
        (*accounts.rent_collector.key, keys.rent_collector),
        (*accounts.system_program.key, keys.system_program),
    ] {
        if actual != expected {
            return Err((actual, expected));
        }
    }
    Ok(())
}
