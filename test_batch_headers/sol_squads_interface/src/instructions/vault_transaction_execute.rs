//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-06
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

use super::*;
///Execute a vault transaction.
///The transaction must be `Approved`.
pub const VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN: usize = 4;
///Accounts struct for the vaultTransactionExecute instruction
#[derive(Copy, Clone, Debug)]
pub struct VaultTransactionExecuteAccounts<'me, 'info> {
    pub multisig: &'me AccountInfo<'info>,
    ///The proposal account associated with the transaction.
    pub proposal: &'me AccountInfo<'info>,
    ///The transaction to execute.
    pub transaction: &'me AccountInfo<'info>,
    pub member: &'me AccountInfo<'info>,
}
///Public keys struct for the vaultTransactionExecute instruction
#[derive(Copy, Clone, Debug, PartialEq)]
pub struct VaultTransactionExecuteKeys {
    pub multisig: Pubkey,
    ///The proposal account associated with the transaction.
    pub proposal: Pubkey,
    ///The transaction to execute.
    pub transaction: Pubkey,
    pub member: Pubkey,
}
impl From<VaultTransactionExecuteAccounts<'_, '_>> for VaultTransactionExecuteKeys {
    fn from(accounts: VaultTransactionExecuteAccounts) -> Self {
        Self {
            multisig: *accounts.multisig.key,
            proposal: *accounts.proposal.key,
            transaction: *accounts.transaction.key,
            member: *accounts.member.key,
        }
    }
}
impl From<VaultTransactionExecuteKeys>
    for [AccountMeta; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]
{
    fn from(keys: VaultTransactionExecuteKeys) -> Self {
        [
            AccountMeta {
                pubkey: keys.multisig,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.proposal,
                is_signer: false,
                is_writable: true,
            },
            AccountMeta {
                pubkey: keys.transaction,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.member,
                is_signer: true,
                is_writable: false,
            },
        ]
    }
}
impl From<[Pubkey; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]> for VaultTransactionExecuteKeys {
    fn from(pubkeys: [Pubkey; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            multisig: pubkeys[0],
            proposal: pubkeys[1],
            transaction: pubkeys[2],
            member: pubkeys[3],
        }
    }
}
impl From<&[String]> for VaultTransactionExecuteKeys {
    fn from(pubkeys: &[String]) -> Self {
        Self {
            multisig: pubkeys[0].parse().unwrap(),
            proposal: pubkeys[1].parse().unwrap(),
            transaction: pubkeys[2].parse().unwrap(),
            member: pubkeys[3].parse().unwrap(),
        }
    }
}
impl<'info> From<VaultTransactionExecuteAccounts<'_, 'info>>
    for [AccountInfo<'info>; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]
{
    fn from(accounts: VaultTransactionExecuteAccounts<'_, 'info>) -> Self {
        [
            accounts.multisig.clone(),
            accounts.proposal.clone(),
            accounts.transaction.clone(),
            accounts.member.clone(),
        ]
    }
}
impl<'me, 'info> From<&'me [AccountInfo<'info>; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]>
    for VaultTransactionExecuteAccounts<'me, 'info>
{
    fn from(arr: &'me [AccountInfo<'info>; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            multisig: &arr[0],
            proposal: &arr[1],
            transaction: &arr[2],
            member: &arr[3],
        }
    }
}
pub const VAULT_TRANSACTION_EXECUTE_IX_DISCM: u8 = 17u8;
#[derive(Clone, Debug, PartialEq)]
pub struct VaultTransactionExecuteIxData;
impl VaultTransactionExecuteIxData {
    pub fn deserialize(buf: &[u8]) -> std::io::Result<Self> {
        if buf.is_empty() {
            return Err(std::io::Error::new(
                std::io::ErrorKind::InvalidData,
                "Instruction data is empty",
            ));
        }
        let maybe_discm = buf[0];
        if maybe_discm != VAULT_TRANSACTION_EXECUTE_IX_DISCM {
            return Err(std::io::Error::new(
                std::io::ErrorKind::Other,
                format!(
                    "discm does not match. Expected: {}. Received: {}",
                    VAULT_TRANSACTION_EXECUTE_IX_DISCM, maybe_discm
                ),
            ));
        }
        Ok(Self)
    }
    pub fn serialize<W: std::io::Write>(&self, mut writer: W) -> std::io::Result<()> {
        writer.write_all(&[VAULT_TRANSACTION_EXECUTE_IX_DISCM])
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        let mut data = Vec::new();
        self.serialize(&mut data)?;
        Ok(data)
    }
}
pub fn vault_transaction_execute_ix_with_program_id(
    program_id: Pubkey,
    keys: VaultTransactionExecuteKeys,
) -> std::io::Result<Instruction> {
    let metas: [AccountMeta; VAULT_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN] = keys.into();
    Ok(Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: VaultTransactionExecuteIxData.try_to_vec()?,
    })
}
pub fn vault_transaction_execute_ix(
    keys: VaultTransactionExecuteKeys,
) -> std::io::Result<Instruction> {
    vault_transaction_execute_ix_with_program_id(crate::ID, keys)
}
pub fn vault_transaction_execute_invoke_with_program_id(
    program_id: Pubkey,
    accounts: VaultTransactionExecuteAccounts<'_, '_>,
) -> ProgramResult {
    let keys: VaultTransactionExecuteKeys = accounts.into();
    let ix = vault_transaction_execute_ix_with_program_id(program_id, keys)?;
    invoke_instruction(&ix, accounts)
}
pub fn vault_transaction_execute_invoke(
    accounts: VaultTransactionExecuteAccounts<'_, '_>,
) -> ProgramResult {
    vault_transaction_execute_invoke_with_program_id(crate::ID, accounts)
}
pub fn vault_transaction_execute_invoke_signed_with_program_id(
    program_id: Pubkey,
    accounts: VaultTransactionExecuteAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    let keys: VaultTransactionExecuteKeys = accounts.into();
    let ix = vault_transaction_execute_ix_with_program_id(program_id, keys)?;
    invoke_instruction_signed(&ix, accounts, seeds)
}
pub fn vault_transaction_execute_invoke_signed(
    accounts: VaultTransactionExecuteAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    vault_transaction_execute_invoke_signed_with_program_id(crate::ID, accounts, seeds)
}
pub fn vault_transaction_execute_verify_account_keys(
    accounts: VaultTransactionExecuteAccounts<'_, '_>,
    keys: VaultTransactionExecuteKeys,
) -> Result<(), (Pubkey, Pubkey)> {
    for (actual, expected) in [
        (*accounts.multisig.key, keys.multisig),
        (*accounts.proposal.key, keys.proposal),
        (*accounts.transaction.key, keys.transaction),
        (*accounts.member.key, keys.member),
    ] {
        if actual != expected {
            return Err((actual, expected));
        }
    }
    Ok(())
}
