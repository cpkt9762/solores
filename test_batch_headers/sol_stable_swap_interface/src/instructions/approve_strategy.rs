//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-06
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

use super::*;
pub const APPROVE_STRATEGY_IX_ACCOUNTS_LEN: usize = 2;
///Accounts struct for the approve_strategy instruction
#[derive(Copy, Clone, Debug)]
pub struct ApproveStrategyAccounts<'me, 'info> {
    pub admin_only: &'me AccountInfo<'info>,
    pub strategy: &'me AccountInfo<'info>,
}
///Public keys struct for the approve_strategy instruction
#[derive(Copy, Clone, Debug, PartialEq)]
pub struct ApproveStrategyKeys {
    pub admin_only: Pubkey,
    pub strategy: Pubkey,
}
impl From<ApproveStrategyAccounts<'_, '_>> for ApproveStrategyKeys {
    fn from(accounts: ApproveStrategyAccounts) -> Self {
        Self {
            admin_only: *accounts.admin_only.key,
            strategy: *accounts.strategy.key,
        }
    }
}
impl From<ApproveStrategyKeys> for [AccountMeta; APPROVE_STRATEGY_IX_ACCOUNTS_LEN] {
    fn from(keys: ApproveStrategyKeys) -> Self {
        [
            AccountMeta {
                pubkey: keys.admin_only,
                is_signer: false,
                is_writable: false,
            },
            AccountMeta {
                pubkey: keys.strategy,
                is_signer: false,
                is_writable: true,
            },
        ]
    }
}
impl From<[Pubkey; APPROVE_STRATEGY_IX_ACCOUNTS_LEN]> for ApproveStrategyKeys {
    fn from(pubkeys: [Pubkey; APPROVE_STRATEGY_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            admin_only: pubkeys[0],
            strategy: pubkeys[1],
        }
    }
}
impl From<&[String]> for ApproveStrategyKeys {
    fn from(pubkeys: &[String]) -> Self {
        Self {
            admin_only: pubkeys[0].parse().unwrap(),
            strategy: pubkeys[1].parse().unwrap(),
        }
    }
}
impl<'info> From<ApproveStrategyAccounts<'_, 'info>>
    for [AccountInfo<'info>; APPROVE_STRATEGY_IX_ACCOUNTS_LEN]
{
    fn from(accounts: ApproveStrategyAccounts<'_, 'info>) -> Self {
        [accounts.admin_only.clone(), accounts.strategy.clone()]
    }
}
impl<'me, 'info> From<&'me [AccountInfo<'info>; APPROVE_STRATEGY_IX_ACCOUNTS_LEN]>
    for ApproveStrategyAccounts<'me, 'info>
{
    fn from(arr: &'me [AccountInfo<'info>; APPROVE_STRATEGY_IX_ACCOUNTS_LEN]) -> Self {
        Self {
            admin_only: &arr[0],
            strategy: &arr[1],
        }
    }
}
pub const APPROVE_STRATEGY_IX_DISCM: [u8; 8] = [7, 141, 162, 60, 71, 115, 26, 146];
#[derive(Clone, Debug, PartialEq)]
pub struct ApproveStrategyIxData;
impl ApproveStrategyIxData {
    pub fn deserialize(buf: &[u8]) -> std::io::Result<Self> {
        let mut reader = buf;
        let mut maybe_discm = [0u8; 8];
        reader.read_exact(&mut maybe_discm)?;
        if maybe_discm != APPROVE_STRATEGY_IX_DISCM {
            return Err(std::io::Error::new(
                std::io::ErrorKind::Other,
                format!(
                    "discm does not match. Expected: {:?}. Received: {:?}",
                    APPROVE_STRATEGY_IX_DISCM, maybe_discm
                ),
            ));
        }
        Ok(Self)
    }
    pub fn serialize<W: std::io::Write>(&self, mut writer: W) -> std::io::Result<()> {
        writer.write_all(&APPROVE_STRATEGY_IX_DISCM)
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        let mut data = Vec::new();
        self.serialize(&mut data)?;
        Ok(data)
    }
}
pub fn approve_strategy_ix_with_program_id(
    program_id: Pubkey,
    keys: ApproveStrategyKeys,
) -> std::io::Result<Instruction> {
    let metas: [AccountMeta; APPROVE_STRATEGY_IX_ACCOUNTS_LEN] = keys.into();
    Ok(Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: ApproveStrategyIxData.try_to_vec()?,
    })
}
pub fn approve_strategy_ix(keys: ApproveStrategyKeys) -> std::io::Result<Instruction> {
    approve_strategy_ix_with_program_id(crate::ID, keys)
}
pub fn approve_strategy_invoke_with_program_id(
    program_id: Pubkey,
    accounts: ApproveStrategyAccounts<'_, '_>,
) -> ProgramResult {
    let keys: ApproveStrategyKeys = accounts.into();
    let ix = approve_strategy_ix_with_program_id(program_id, keys)?;
    invoke_instruction(&ix, accounts)
}
pub fn approve_strategy_invoke(accounts: ApproveStrategyAccounts<'_, '_>) -> ProgramResult {
    approve_strategy_invoke_with_program_id(crate::ID, accounts)
}
pub fn approve_strategy_invoke_signed_with_program_id(
    program_id: Pubkey,
    accounts: ApproveStrategyAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    let keys: ApproveStrategyKeys = accounts.into();
    let ix = approve_strategy_ix_with_program_id(program_id, keys)?;
    invoke_instruction_signed(&ix, accounts, seeds)
}
pub fn approve_strategy_invoke_signed(
    accounts: ApproveStrategyAccounts<'_, '_>,
    seeds: &[&[&[u8]]],
) -> ProgramResult {
    approve_strategy_invoke_signed_with_program_id(crate::ID, accounts, seeds)
}
pub fn approve_strategy_verify_account_keys(
    accounts: ApproveStrategyAccounts<'_, '_>,
    keys: ApproveStrategyKeys,
) -> Result<(), (Pubkey, Pubkey)> {
    for (actual, expected) in [
        (*accounts.admin_only.key, keys.admin_only),
        (*accounts.strategy.key, keys.strategy),
    ] {
        if actual != expected {
            return Err((actual, expected));
        }
    }
    Ok(())
}
