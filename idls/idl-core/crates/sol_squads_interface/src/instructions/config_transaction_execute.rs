//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-16
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

//!Instruction: configTransactionExecute
///Execute a config transaction.
///The transaction must be `Approved`.
#[allow(unused_imports)]
use solana_pubkey::Pubkey;
pub const CONFIG_TRANSACTION_EXECUTE_IX_DISCM: [u8; 8] =
    [209u8, 6u8, 67u8, 145u8, 173u8, 217u8, 162u8, 1u8];
pub const CONFIG_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN: usize = 6;
///Execute a config transaction.
///The transaction must be `Approved`.
#[derive(borsh::BorshDeserialize, borsh::BorshSerialize, Clone, Debug, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ConfigTransactionExecuteIxData {
    pub discriminator: [u8; 8],
}
impl Default for ConfigTransactionExecuteIxData {
    fn default() -> Self {
        Self {
            discriminator: CONFIG_TRANSACTION_EXECUTE_IX_DISCM,
        }
    }
}
impl ConfigTransactionExecuteIxData {
    pub fn new() -> Self {
        Self {
            discriminator: Self::discriminator(),
        }
    }
    pub fn from_bytes(buf: &[u8]) -> std::io::Result<Self> {
        borsh::BorshDeserialize::deserialize(&mut &buf[..])
    }
    pub fn discriminator() -> [u8; 8] {
        CONFIG_TRANSACTION_EXECUTE_IX_DISCM
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        borsh::to_vec(self)
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [format!(
                "\"discriminator\":[{}]",
                self.discriminator
                    .iter()
                    .map(|x| x.to_string())
                    .collect::<Vec<_>>()
                    .join(",")
            )]
            .join(",")
        )
    }
}
#[derive(Copy, Clone, Debug, PartialEq, Default)]
pub struct ConfigTransactionExecuteKeys {
    pub multisig: Pubkey,
    pub member: Pubkey,
    pub proposal: Pubkey,
    pub transaction: Pubkey,
    pub rent_payer: Pubkey,
    pub system_program: Pubkey,
}
impl From<&[Pubkey]> for ConfigTransactionExecuteKeys {
    fn from(pubkeys: &[Pubkey]) -> Self {
        Self {
            multisig: pubkeys[0],
            member: pubkeys[1],
            proposal: pubkeys[2],
            transaction: pubkeys[3],
            rent_payer: pubkeys[4],
            system_program: pubkeys[5],
        }
    }
}
impl ConfigTransactionExecuteKeys {
    /// Convert Keys to Vec<Pubkey>
    pub fn to_vec(&self) -> Vec<Pubkey> {
        vec![
            self.multisig,
            self.member,
            self.proposal,
            self.transaction,
            self.rent_payer,
            self.system_program,
        ]
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [
                format!("\"{}\":\"{}\"", "multisig", self.multisig),
                format!("\"{}\":\"{}\"", "member", self.member),
                format!("\"{}\":\"{}\"", "proposal", self.proposal),
                format!("\"{}\":\"{}\"", "transaction", self.transaction),
                format!("\"{}\":\"{}\"", "rentPayer", self.rent_payer),
                format!("\"{}\":\"{}\"", "systemProgram", self.system_program)
            ]
            .join(",")
        )
    }
}
impl From<ConfigTransactionExecuteKeys>
    for [solana_instruction::AccountMeta; CONFIG_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN]
{
    fn from(keys: ConfigTransactionExecuteKeys) -> Self {
        [
            solana_instruction::AccountMeta::new(keys.multisig, false),
            solana_instruction::AccountMeta::new(keys.member, false),
            solana_instruction::AccountMeta::new(keys.proposal, false),
            solana_instruction::AccountMeta::new(keys.transaction, false),
            solana_instruction::AccountMeta::new(keys.rent_payer, false),
            solana_instruction::AccountMeta::new(keys.system_program, false),
        ]
    }
}
pub fn config_transaction_execute_ix_with_program_id(
    program_id: Pubkey,
    keys: ConfigTransactionExecuteKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    let metas: [solana_instruction::AccountMeta; CONFIG_TRANSACTION_EXECUTE_IX_ACCOUNTS_LEN] =
        keys.into();
    let args = ConfigTransactionExecuteIxData::default();
    Ok(solana_instruction::Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: args.try_to_vec()?,
    })
}
pub fn config_transaction_execute_ix(
    keys: ConfigTransactionExecuteKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    config_transaction_execute_ix_with_program_id(crate::ID, keys)
}
