//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-16
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

//!Instruction: vaultBatchTransactionAccountClose
///Closes a `VaultBatchTransaction` belonging to the `batch` and `proposal`.
///`transaction` can be closed if either:
///- it's marked as executed within the `batch`;
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
#[allow(unused_imports)]
use solana_pubkey::Pubkey;
pub const VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM: [u8; 8] =
    [119u8, 108u8, 173u8, 144u8, 88u8, 110u8, 55u8, 1u8];
pub const VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN: usize = 6;
///Closes a `VaultBatchTransaction` belonging to the `batch` and `proposal`.
///`transaction` can be closed if either:
///- it's marked as executed within the `batch`;
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
#[derive(borsh::BorshDeserialize, borsh::BorshSerialize, Clone, Debug, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct VaultBatchTransactionAccountCloseIxData {
    pub discriminator: [u8; 8],
}
impl Default for VaultBatchTransactionAccountCloseIxData {
    fn default() -> Self {
        Self {
            discriminator: VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM,
        }
    }
}
impl VaultBatchTransactionAccountCloseIxData {
    pub fn new() -> Self {
        Self {
            discriminator: Self::discriminator(),
        }
    }
    pub fn from_bytes(buf: &[u8]) -> std::io::Result<Self> {
        borsh::BorshDeserialize::deserialize(&mut &buf[..])
    }
    pub fn discriminator() -> [u8; 8] {
        VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_DISCM
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        borsh::to_vec(self)
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [format!(
                "\"discriminator\":[{}]",
                self.discriminator
                    .iter()
                    .map(|x| x.to_string())
                    .collect::<Vec<_>>()
                    .join(",")
            )]
            .join(",")
        )
    }
}
#[derive(Copy, Clone, Debug, PartialEq, Default)]
pub struct VaultBatchTransactionAccountCloseKeys {
    pub multisig: Pubkey,
    pub proposal: Pubkey,
    pub batch: Pubkey,
    pub transaction: Pubkey,
    pub rent_collector: Pubkey,
    pub system_program: Pubkey,
}
impl From<&[Pubkey]> for VaultBatchTransactionAccountCloseKeys {
    fn from(pubkeys: &[Pubkey]) -> Self {
        Self {
            multisig: pubkeys[0],
            proposal: pubkeys[1],
            batch: pubkeys[2],
            transaction: pubkeys[3],
            rent_collector: pubkeys[4],
            system_program: pubkeys[5],
        }
    }
}
impl VaultBatchTransactionAccountCloseKeys {
    /// Convert Keys to Vec<Pubkey>
    pub fn to_vec(&self) -> Vec<Pubkey> {
        vec![
            self.multisig,
            self.proposal,
            self.batch,
            self.transaction,
            self.rent_collector,
            self.system_program,
        ]
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [
                format!("\"{}\":\"{}\"", "multisig", self.multisig),
                format!("\"{}\":\"{}\"", "proposal", self.proposal),
                format!("\"{}\":\"{}\"", "batch", self.batch),
                format!("\"{}\":\"{}\"", "transaction", self.transaction),
                format!("\"{}\":\"{}\"", "rentCollector", self.rent_collector),
                format!("\"{}\":\"{}\"", "systemProgram", self.system_program)
            ]
            .join(",")
        )
    }
}
impl From<VaultBatchTransactionAccountCloseKeys>
    for [solana_instruction::AccountMeta; VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(keys: VaultBatchTransactionAccountCloseKeys) -> Self {
        [
            solana_instruction::AccountMeta::new(keys.multisig, false),
            solana_instruction::AccountMeta::new(keys.proposal, false),
            solana_instruction::AccountMeta::new(keys.batch, false),
            solana_instruction::AccountMeta::new(keys.transaction, false),
            solana_instruction::AccountMeta::new(keys.rent_collector, false),
            solana_instruction::AccountMeta::new(keys.system_program, false),
        ]
    }
}
pub fn vault_batch_transaction_account_close_ix_with_program_id(
    program_id: Pubkey,
    keys: VaultBatchTransactionAccountCloseKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    let metas: [solana_instruction::AccountMeta;
        VAULT_BATCH_TRANSACTION_ACCOUNT_CLOSE_IX_ACCOUNTS_LEN] = keys.into();
    let args = VaultBatchTransactionAccountCloseIxData::default();
    Ok(solana_instruction::Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: args.try_to_vec()?,
    })
}
pub fn vault_batch_transaction_account_close_ix(
    keys: VaultBatchTransactionAccountCloseKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    vault_batch_transaction_account_close_ix_with_program_id(crate::ID, keys)
}
