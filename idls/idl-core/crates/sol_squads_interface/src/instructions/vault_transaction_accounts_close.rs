//! This file was automatically generated by Solores
//! Solana IDL to Rust interface generator
//!
//! GitHub: https://github.com/cpkt9762/solores
//!
//! Generated on: 2025-08-16
//!
//! DO NOT EDIT - This file is auto-generated
//! Changes will be lost when regenerating from IDL

//!Instruction: vaultTransactionAccountsClose
///Closes a `VaultTransaction` and the corresponding `Proposal`.
///`transaction` can be closed if either:
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
#[allow(unused_imports)]
use solana_pubkey::Pubkey;
pub const VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM: [u8; 8] =
    [8u8, 62u8, 213u8, 86u8, 163u8, 25u8, 120u8, 84u8];
pub const VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN: usize = 5;
///Closes a `VaultTransaction` and the corresponding `Proposal`.
///`transaction` can be closed if either:
///- the `proposal` is in a terminal state: `Executed`, `Rejected`, or `Cancelled`.
///- the `proposal` is stale and not `Approved`.
#[derive(borsh::BorshDeserialize, borsh::BorshSerialize, Clone, Debug, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct VaultTransactionAccountsCloseIxData {
    pub discriminator: [u8; 8],
}
impl Default for VaultTransactionAccountsCloseIxData {
    fn default() -> Self {
        Self {
            discriminator: VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM,
        }
    }
}
impl VaultTransactionAccountsCloseIxData {
    pub fn new() -> Self {
        Self {
            discriminator: Self::discriminator(),
        }
    }
    pub fn from_bytes(buf: &[u8]) -> std::io::Result<Self> {
        borsh::BorshDeserialize::deserialize(&mut &buf[..])
    }
    pub fn discriminator() -> [u8; 8] {
        VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_DISCM
    }
    pub fn try_to_vec(&self) -> std::io::Result<Vec<u8>> {
        borsh::to_vec(self)
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [format!(
                "\"discriminator\":[{}]",
                self.discriminator
                    .iter()
                    .map(|x| x.to_string())
                    .collect::<Vec<_>>()
                    .join(",")
            )]
            .join(",")
        )
    }
}
#[derive(Copy, Clone, Debug, PartialEq, Default)]
pub struct VaultTransactionAccountsCloseKeys {
    pub multisig: Pubkey,
    pub proposal: Pubkey,
    pub transaction: Pubkey,
    pub rent_collector: Pubkey,
    pub system_program: Pubkey,
}
impl From<&[Pubkey]> for VaultTransactionAccountsCloseKeys {
    fn from(pubkeys: &[Pubkey]) -> Self {
        Self {
            multisig: pubkeys[0],
            proposal: pubkeys[1],
            transaction: pubkeys[2],
            rent_collector: pubkeys[3],
            system_program: pubkeys[4],
        }
    }
}
impl VaultTransactionAccountsCloseKeys {
    /// Convert Keys to Vec<Pubkey>
    pub fn to_vec(&self) -> Vec<Pubkey> {
        vec![
            self.multisig,
            self.proposal,
            self.transaction,
            self.rent_collector,
            self.system_program,
        ]
    }
    /// Manual JSON serialization
    #[cfg(feature = "serde")]
    pub fn to_json(&self) -> String {
        format!(
            "{{{}}}",
            [
                format!("\"{}\":\"{}\"", "multisig", self.multisig),
                format!("\"{}\":\"{}\"", "proposal", self.proposal),
                format!("\"{}\":\"{}\"", "transaction", self.transaction),
                format!("\"{}\":\"{}\"", "rentCollector", self.rent_collector),
                format!("\"{}\":\"{}\"", "systemProgram", self.system_program)
            ]
            .join(",")
        )
    }
}
impl From<VaultTransactionAccountsCloseKeys>
    for [solana_instruction::AccountMeta; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN]
{
    fn from(keys: VaultTransactionAccountsCloseKeys) -> Self {
        [
            solana_instruction::AccountMeta::new(keys.multisig, false),
            solana_instruction::AccountMeta::new(keys.proposal, false),
            solana_instruction::AccountMeta::new(keys.transaction, false),
            solana_instruction::AccountMeta::new(keys.rent_collector, false),
            solana_instruction::AccountMeta::new(keys.system_program, false),
        ]
    }
}
pub fn vault_transaction_accounts_close_ix_with_program_id(
    program_id: Pubkey,
    keys: VaultTransactionAccountsCloseKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    let metas: [solana_instruction::AccountMeta; VAULT_TRANSACTION_ACCOUNTS_CLOSE_IX_ACCOUNTS_LEN] =
        keys.into();
    let args = VaultTransactionAccountsCloseIxData::default();
    Ok(solana_instruction::Instruction {
        program_id,
        accounts: Vec::from(metas),
        data: args.try_to_vec()?,
    })
}
pub fn vault_transaction_accounts_close_ix(
    keys: VaultTransactionAccountsCloseKeys,
) -> Result<solana_instruction::Instruction, std::io::Error> {
    vault_transaction_accounts_close_ix_with_program_id(crate::ID, keys)
}
